// This file used to expose a modal-bottom-sheet folder picker. It was
// replaced by the dialog-based flow in `folder_dialogs.dart` because the
// modal was unreliable on some Android devices (barrier painted, content
// never did). Kept as an empty file so any lingering imports fail loudly
// at analysis time rather than silently referring to a stale widget.
